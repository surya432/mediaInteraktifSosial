import 'package:flutter/material.dart';

class TujuanScreen extends StatelessWidget {
  const TujuanScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:Text("TujuanScreen")
    );
  }
}